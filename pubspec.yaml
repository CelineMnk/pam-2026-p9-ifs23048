
name: pam_p9_2026_ifs23048
description: "Delcom Motivations with AI Generate"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  flutter_markdown: ^0.7.3
  cupertino_icons: ^1.0.6
  intl: ^0.19.0  # ← tambahkan ini

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  launcher_name: ^1.0.2

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"

launcher_name:
  default: "Delcom Motivation"
  id: "Delcom Motivation"

flutter:
  uses-material-design: true
  assets:
    - assets/images/
